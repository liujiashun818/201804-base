/**
 * 纯函数  把一组件映射为另外一组值
 * g = f(x)
 * 1. 输入如果一定，输出也是一定的 y = x+1;
 * 2. 在函数内部不能影响函数外的变量
 */
function add(x) {
    return x + Math.random();
}
let a = 10;
//修改了函数外的变量
//修改了参数的值
function minus(b) {
    a = 20;
    b++;
}
//1.输入一定，输出一定，2 不能修改输入的参数 3，不能修改本函数作用域外的变量的值

